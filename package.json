{
  "name": "tododev-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir ./build"
  },
  "dependencies": {
    "hono": "^4.4.10"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
